# AllToolkit V4

A responsive, privacy-first Next.js toolkit with 120 browser-based tools.

## Run locally

```bash
npm install
npm run dev
```

## Production check

```bash
npm run build
```

Set `NEXT_PUBLIC_SITE_URL` in `.env.local` before deployment so canonical URLs and the sitemap use your real domain.
